#!/bin/bash
# run with sudo
# installs a set of tools to connect disconnect list and verify the wifi

# the path where the commands will be installed 
BIN_PATH='/usr/local/bin'

# root path to the directory
ROOT_DIR="$(cd "$(dirname "$0")/../../"; pwd)"

# path to the scripts
SCRIPTS="$ROOT_DIR/bin/"


